# cmocka integration for CMake

A CMake module for generating runners for cmocka based unit tests.

```
$ cd cmake-cmocka
$ mkdir build
$ cmake -G "<generator supported by your platform>" ..
$ cmake --build .
$ ./cmocka-test
```
